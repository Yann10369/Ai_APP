# AI-Photo-Eraser 模型模块依赖
# 核心
numpy>=1.26
opencv-python>=4.9
Pillow>=10.0
PyYAML>=6.0
loguru>=0.7

# 端侧 LaMa 推理
onnxruntime>=1.17;       # CPU 版
# onnxruntime-gpu>=1.17  # GPU 版（取消注释）

# 云端 SDK（可选）
httpx>=0.27

# 大模型辅助生成蒙版（可选）
pydantic>=2.5
pydantic-settings>=2.1

# 评估（可选）
scikit-image>=0.22        # SSIM
lpips>=0.1.4              # LPIPS